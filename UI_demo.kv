FloatLayout:
    Spinner:
        size_hint: None, none
        size: 100, 44
        pos_hint: {'center': (.5, .5)}
        text: 'Home'
        values: 'Home', 'Work', 'Other', 'Custom'
        on_value:
            print: "The spinner {} has text {}".format(self, self.value)